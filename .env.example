# URL of the FastAPI backend (used by the Next.js frontend)
NEXT_PUBLIC_BACKEND_URL=http://127.0.0.1:8000

# Set to 1 to enable verbose backend logging (never commit real tokens or passwords)
VALORANT_DEBUG=0
